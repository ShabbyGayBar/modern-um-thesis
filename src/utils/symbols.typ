/// Shortcut for upright Greek letter pi.
///
/// *Example:*
///
/// ```example
/// >>> #let ppi = $upright(pi)$
/// $A = ppi r^2$
/// ```
/// 
/// -> content
#let ppi = $upright(pi)$

/// Shortcut for upright e, the base of the natural logarithm.
///
/// *Example:*
///
/// ```example
/// >>> #let ee = $upright(e)$
/// $ee^x$
/// ```
/// 
/// -> content
#let ee = $upright(e)$

/// Shortcut for upright imaginary unit i.
///
/// *Example:*
/// 
/// ```example
/// >>> #let ii = $upright(i)$
/// $1+2ii$
/// ```
/// 
/// -> content
#let ii = $upright(i)$
